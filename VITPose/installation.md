uv pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
uv pip install transformers accelerate pillow numpy supervision
uv pip install huggingface_hub[hf_xet] #makes downloads faster 
